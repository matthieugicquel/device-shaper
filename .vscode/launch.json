{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        // "${workspaceFolder}/test-app",
        "--extensionDevelopmentPath=${workspaceFolder}/packages/vzr-vscode"
      ],
      "outFiles": ["${workspaceFolder}/packages/vzr-vscode/out/**/*.js"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
